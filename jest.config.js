// jest.config.js

module.exports = {
    // Other Jest configuration options...
  
    // Configure the test match pattern to only include source files
    testMatch: ["<rootDir>/tests/**/*.test.ts"],
  };